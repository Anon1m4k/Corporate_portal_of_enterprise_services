@page
@model ServiceHub.Pages.Transport.ScheduleModel
@{
    ViewData["Title"] = "Расписание трансфера";
}

<div class="container mt-4 fade-in">
    <div class="d-flex justify-content-between align-items-center mb-5">
        <div>
            <h2 class="section-title mb-1"><i class="fas fa-bus-alt me-3"></i>Регулярные маршруты</h2>
            <p class="text-muted mb-0">Расписание корпоративного транспорта и трансферов</p>
        </div>
        <a asp-page="/Transport/Index" class="btn btn-outline-primary rounded-pill px-4">
            <i class="fas fa-arrow-left me-2"></i>Мои заявки
        </a>
    </div>

    @if (!Model.Routes.Any())
    {
        <div class="card border-0 shadow-xl rounded-4 p-5 text-center">
            <div class="icon-box mx-auto mb-3 opacity-25">
                <i class="fas fa-calendar-times"></i>
            </div>
            <h5 class="text-muted">Активных маршрутов не найдено</h5>
            <p class="mb-0">Обратитесь к администратору для уточнения расписания.</p>
        </div>
    }
    else
    {
        <div class="row row-cols-1 row-cols-lg-2 g-4">
            @foreach (var route in Model.Routes)
            {
                <div class="col fade-in-up">
                    <div class="card h-100 border-0 shadow-xl rounded-4 overflow-hidden">
                        <div class="card-header bg-white border-0 p-4">
                            <div class="d-flex justify-content-between align-items-start">
                                <div>
                                    <h4 class="fw-bold mb-1 text-primary">@route.Name</h4>
                                    <span class="badge bg-secondary bg-opacity-10 text-secondary border">Код: RT-@route.Id</span>
                                </div>
                                <div class="bg-primary bg-opacity-10 text-primary p-3 rounded-4">
                                    <i class="fas fa-route fa-xl"></i>
                                </div>
                            </div>
                        </div>
                        <div class="card-body p-4 pt-0">
                            @if (!string.IsNullOrEmpty(route.Description))
                            {
                                <div class="bg-light rounded-16 p-3 mb-4 border-start border-4 border-primary">
                                    <p class="small mb-0 text-muted">@route.Description</p>
                                </div>
                            }

                            <p class="mb-4">
                                <i class="fas fa-shuttle-van text-primary me-2"></i>
                                <strong>Транспорт:</strong>
                                @if (route.Car != null)
                                {
                                    <span class="text-dark">@($"{route.Car.Brand} {route.Car.Model}")</span>
                                    <span class="badge bg-info bg-opacity-10 text-primary ms-1">@route.Car.LicensePlate</span>
                                }
                                else
                                {
                                    <span class="text-muted">Не назначен</span>
                                }
                            </p>

                            <div class="schedule-timeline">
                                <h6 class="fw-bold mb-3"><i class="far fa-clock me-2"></i>Остановки и время:</h6>
                                <div class="table-responsive">
                                    <table class="table table-sm table-borderless align-middle">
                                        <tbody>
                                            @foreach (var stop in route.Stops.OrderBy(s => s.Order))
                                            {
                                                <tr>
                                                    <td width="5%" class="text-center">
                                                        <div class="d-flex flex-column align-items-center">
                                                            <i class="fas fa-circle text-primary" style="font-size: 8px;"></i>
                                                            @if (stop != route.Stops.OrderBy(s => s.Order).Last())
                                                            {
                                                                <div style="width: 1px; height: 25px; border-left: 1px dashed #cbd5e1;"></div>
                                                            }
                                                        </div>
                                                    </td>
                                                    <td class="ps-3 py-2 fw-medium">@stop.Address</td>
                                                    <td class="text-end py-2">
                                                        <span class="badge bg-primary rounded-pill">@stop.ArrivalTime.ToString(@"hh\:mm")</span>
                                                    </td>
                                                </tr>
                                            }
                                        </tbody>
                                    </table>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            }
        </div>
    }
</div>