@page
@model ServiceHub.Pages.Transport.CreateModel
@{
    ViewData["Title"] = "Новая заявка на транспорт";
}

<div class="container mt-4">
    <div class="card border-0 shadow">
        <div class="card-body p-4">
            <h2 class="mb-4"><i class="fas fa-plus-circle me-2"></i>Служебная поездка</h2>
            <form method="post">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                <div class="row">
                    <div class="col-md-6 mb-3">
                        <label asp-for="TransportRequest.TripDateTime" class="form-label"><i class="fas fa-calendar-alt me-2"></i>Дата и время</label>
                        <input asp-for="TransportRequest.TripDateTime" class="form-control" type="datetime-local" />
                        <span asp-validation-for="TransportRequest.TripDateTime" class="text-danger"></span>
                    </div>

                    <div class="col-md-6 mb-3">
                        <label asp-for="TransportRequest.PassengerCount" class="form-label"><i class="fas fa-users me-2"></i>Количество пассажиров</label>
                        <input asp-for="TransportRequest.PassengerCount" class="form-control" type="number" />
                        <span asp-validation-for="TransportRequest.PassengerCount" class="text-danger"></span>
                    </div>
                </div>

                <div class="row">
                    <div class="col-md-6 mb-3">
                        <label asp-for="TransportRequest.StartPoint" class="form-label"><i class="fas fa-map-marker-alt me-2"></i>Откуда</label>
                        <input asp-for="TransportRequest.StartPoint" class="form-control" />
                        <span asp-validation-for="TransportRequest.StartPoint" class="text-danger"></span>
                    </div>

                    <div class="col-md-6 mb-3">
                        <label asp-for="TransportRequest.EndPoint" class="form-label"><i class="fas fa-flag-checkered me-2"></i>Куда</label>
                        <input asp-for="TransportRequest.EndPoint" class="form-control" />
                        <span asp-validation-for="TransportRequest.EndPoint" class="text-danger"></span>
                    </div>
                </div>

                <div class="mb-3">
                    <label asp-for="TransportRequest.Purpose" class="form-label"><i class="fas fa-pencil-alt me-2"></i>Цель поездки</label>
                    <textarea asp-for="TransportRequest.Purpose" class="form-control" rows="2"></textarea>
                    <span asp-validation-for="TransportRequest.Purpose" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="TransportRequest.CarId" class="form-label"><i class="fas fa-car me-2"></i>Автомобиль</label>
                    <select asp-for="TransportRequest.CarId" class="form-select" asp-items="Model.CarOptions">
                        <option value="">-- Выберите автомобиль --</option>
                    </select>
                    <span asp-validation-for="TransportRequest.CarId" class="text-danger"></span>
                </div>

                <button type="submit" class="btn btn-primary"><i class="fas fa-paper-plane me-2"></i>Отправить заявку</button>
                <a asp-page="/Transport/Index" class="btn btn-secondary"><i class="fas fa-times me-2"></i>Отмена</a>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}